import datetime

#check for available tasks
#grab a task
#double check that a task was grabbed by this drone and another didnt kipe it
    #if no task claimed, claim one again


now = datetime.datetime.now()
currentTime = now.strftime("%H:%M")
print(currentTime)
